---
phase: 01-stable-extension-id-oauth-hardening
plan: "03"
type: execute
wave: 2
depends_on: [01-01]
files_modified: [scripts/package-store.sh, scripts/test-oauth-package.js, package.json, docs/OAUTH-RUNBOOK.md]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: Every standard/friends package retains the same public key and excludes private credentials."
    - "D-01: Ben receives exact public configuration values and steps for updating both Google OAuth clients after the package is ready."
    - "D-04: The runbook explains expected Testing-mode repeat consent; it does not initiate production verification."
    - "D-02 and D-03: The runbook distinguishes reconnect, offline/setup failures and a permitted personal-account fallback."
  artifacts:
    - path: scripts/test-oauth-package.js
      provides: Public-key derivation, staged-package validation and negative packaging fixtures
      exports: [deriveExtensionId, validatePackageTree]
    - path: scripts/package-store.sh
      provides: Fail-closed stable-key/credential checks on the actual staged files
      contains: test-oauth-package.js
    - path: package.json
      provides: Existing test chain extended with auth and packaging tests
      contains: test-google-auth.js
    - path: docs/OAUTH-RUNBOOK.md
      provides: Public ID/client/redirect values, owner checklist and pending real-account acceptance record
  key_links:
    - from: scripts/package-store.sh
      to: scripts/test-oauth-package.js
      via: Validate staged tree against source manifest before ZIP creation
      pattern: test-oauth-package\.js
    - from: manifest.json
      to: docs/OAUTH-RUNBOOK.md
      via: Derive extension ID from exact shipped public key; record actual Chrome redirect later
      pattern: chromiumapp\.org
    - from: package.json
      to: scripts/test-google-auth.js
      via: Required test chain
      pattern: test-google-auth\.js
  prohibitions:
    - statement: "MUST NOT discard the old installation or its local student work to make a new stable-ID installation appear successful."
      status: unresolved
---
<objective>
Make stable-identity distribution verifiable and owner setup concrete per D-01 through D-04.

**As a** pilot friend on a school-managed Chrome account, **I want to** complete Google sign-in, **so that** my connection keeps working across reinstalls, reloads, and time.
</objective>
<execution_context>
@/Users/vanshkumar/.codex/gsd-core/workflows/execute-plan.md
@/Users/vanshkumar/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@LEARNINGS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-CONTEXT.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-RESEARCH.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-PATTERNS.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-01-SUMMARY.md
</context>
<flagged_assumptions>
AUTH-02 remains category:unclassified, status:unresolved from the probe; derived ID/package equality does not prove Chrome or Console behavior. Research A1 (existing old-ID data) and A2 (owner/account availability) require end-of-phase observations. The descriptor-less prohibition is flagged-unverified; its wording is not machine proof.
</flagged_assumptions>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce permanent identity and credential exclusion in distribution</name>
  <files>scripts/test-oauth-package.js, scripts/package-store.sh, package.json</files>
  <read_first>Read actual manifest key from 01-01; scripts/package-store.sh staged copy/manifest transform/ZIP steps; package.json test chain; .gitignore; scripts/test-google-auth.js runner shape.</read_first>
  <behavior>
    - Source public key parses as RSA SPKI and deterministically yields the expected extension ID; absent/private/malformed key is rejected.
    - A staged manifest with changed/missing key fails before ZIP output; identical public key survives both build variants.
    - A staged .pem, private-key marker or Google client-secret field fails packaging; normal public client IDs remain allowed.
    - Negative fixtures actually exercise validator failure, and cleanup only removes test-owned temporary paths.
  </behavior>
  <action>
Create a dependency-free CommonJS helper/test script exporting deriveExtensionId and validatePackageTree. Default invocation runs a positive source-key check and temporary-tree positive/negative fixtures, printing positive counts and failing if zero assertions run. Provide --stage DIR to inspect the real staged tree and compare its manifest key to the repo manifest; require valid public RSA SPKI, identical DER, same derived ID, unchanged OAuth client/scopes and the expected local-permission transform. Scan staged file names/content for private key material and client-secret fields with explicit diagnostics that reveal paths only. Never log secret values or generate keys from this script.

Call the stage validator from package-store.sh after staging/manifest cleanup and before ZIP creation. Preserve friends build flags, existing allowlist, viewer/offscreen checks and deterministic public identity. Test both variants using isolated fixtures here; 01-04 produces authoritative release archives after parallel code edits finish. Append the real-module auth and packaging tests to the existing package.json test chain without removing current suites or changing dependencies. Keep shell packaging usable directly because this machine has Node but no npm executable. No artifact built concurrently with 01-02 is release evidence.
  </action>
  <acceptance_criteria>
    - The validator has real positive and negative cases and exits nonzero for each unsafe staged fixture.
    - Actual packaging invokes validation before creating either ZIP; keys are never regenerated during packaging.
    - Both new scripts appear in the existing test chain; no dependency or lockfile changes are made.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-oauth-package.js</automated>
    <fails_when>Non-zero exit, zero executed cases, missing negative-fixture assertions, or any FAIL assertion.</fails_when>
    <automated>bash -n scripts/package-store.sh</automated>
    <fails_when>Non-zero exit or any shell syntax diagnostic.</fails_when>
  </verify>
  <done>Distribution fails before publication if the permanent identity changes or private credentials enter the staged files.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare the exact owner setup and non-destructive acceptance runbook</name>
  <files>docs/OAUTH-RUNBOOK.md</files>
  <read_first>Read actual source public key/client IDs, 01-01 summary, existing .gitignore; research stable identity/runtime inventory/Testing constraints; deploy/README.md only as numbered-runbook style precedent.</read_first>
  <action>
Write docs/OAUTH-RUNBOOK.md with current public Chrome client ID, WEB_CLIENT_ID, derived permanent extension ID, expected HTTPS chromiumapp redirect including trailing slash, and explicit pending fields for the ID/redirect observed in real Chrome. State how each value was obtained, not fabricated final values. Explain key custody and exclusive generation already performed; document that public key reuse is permanent and private material is never sent to friends or Google.

Per D-01 place Ben's owner action after code/tests/package readiness: inspect the Chrome Extension client's Item ID and update it to the observed stable extension ID; if Console requires replacement, return the new public client ID for the executor to update manifest.oauth2.client_id and rebuild. Inspect the separate Web application client and register the exact observed getRedirectURL, retaining the existing flow with no client secret. If that client changes, return its public ID for WEB_CLIENT_ID replacement and rebuild. Verify current APIs and pilot Testing audience entries without adding Classroom scopes or requesting production review.

Document load-unpacked from the friends archive, reload/reinstall from a second directory/profile or machine, same-ID checks, explicit permitted personal fallback, real owned test-document read/outline operation, revoked-or-expired grant → Reconnect Google → restored access, and offline/resource-denial checks. Record outcome/date/profile type/build hash without student emails, tokens, documents or screenshots containing personal work. Distinguish revoked/synthetic expiry from seven elapsed days. Per D-04 consent may be needed again after Testing authorization lapses; silent access-token renewal is not a refresh-token implementation.

Protect the existing installation: inventory whether old-ID local work/settings need retention before any replacement; keep the old install and its data until verified; use a separate Chrome profile for stable-ID trials. Never prescribe deleting an old install to fix OAuth, promise automatic storage migration, export bearer tokens, or claim every managed school permits personal login. Explain inherited implicit-flow and local token-store boundaries as residual architecture, not a newly implemented backend. Include an acceptance table with every live check marked pending until observed. Hosting remains Fly.io Phase 1.5, outside these instructions.
  </action>
  <acceptance_criteria>
    - Runbook contains both actual public client identifiers and the key-derived expected ID/redirect; real Chrome/Console observations are clearly pending.
    - Both owner registrations, replacement-ID handling/rebuild, pilot audience, Testing reconnect, policy denial and old-data retention have concrete numbered instructions.
    - No live Google success or seven-day wait is claimed from automated tests.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node -e 'const fs=require("fs"),a=require("assert"),m=require("./manifest.json"),g=fs.readFileSync("lib/google.js","utf8"),d=fs.readFileSync("docs/OAUTH-RUNBOOK.md","utf8"),p=require("./scripts/test-oauth-package.js"); a(d.includes(m.oauth2.client_id)); a(d.includes(g.match(/const WEB_CLIENT_ID\s*=\s*"([^"]+)"/)[1])); a(d.includes(p.deriveExtensionId(m.key))); a(d.includes("chromiumapp.org/")); a(d.includes("Testing")); a(d.includes("pending")); console.log("Runbook public configuration matches source");'</automated>
    <fails_when>Non-zero exit, mismatched/missing public values, or absent success message.</fails_when>
  </verify>
  <done>Ben can review and execute the complete external setup against exact prepared public values, while existing student data and unverified outcomes remain protected.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; high/critical findings block acceptance. Boundaries: source → staged ZIP → friend's installation; local setup → owner Console configuration.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Tampering | Packaged manifest | high | mitigate | Task 1 compares DER/public identity and client configuration before ZIP generation; negative fixtures prove rejection (V2/V11). |
| T-01-11 | Information disclosure | Release archive | high | mitigate | Allowlist plus staged private-key/credential checks; no PEM/private values in docs or output (V14). |
| T-01-12 | Spoofing | OAuth registration | high | mitigate | Task 2 records separate exact clients and redirect; 01-04 requires owner confirmation of both against real Chrome (V10). |
| T-01-13 | Repudiation | Acceptance evidence | medium | mitigate | Build hashes/dates/outcomes and explicit pending fields; no mock/live conflation (V16). |
</threat_model>
<verification>Fixtures and runbook consistency run independently of 01-02. Final full tests, both real ZIPs and extracted-package comparison happen only after both wave-2 plans finish.</verification>
<success_criteria>Stable identity and credential exclusion are enforced by packaging; both OAuth registrations and pilot recovery have an actionable, evidence-ready owner runbook.</success_criteria>
<output>Create .planning/phases/01-stable-extension-id-oauth-hardening/01-03-SUMMARY.md with validator CLI/export contracts, public ID and owner readiness status.</output>
