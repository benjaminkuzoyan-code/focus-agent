---
phase: 01-stable-extension-id-oauth-hardening
plan: "04"
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: [docs/OAUTH-RUNBOOK.md, manifest.json, lib/google.js]
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: Google Auth Platform
    why: D-01 assigns both OAuth client registrations to Ben; real consent requires a permitted pilot account.
    dashboard_config:
      - task: Match Chrome Extension Item ID and Web application redirect to the observed permanent extension identity.
        location: Google Cloud Console -> Google Auth Platform -> Clients
      - task: Confirm permitted pilot test-user access while remaining in Testing.
        location: Google Cloud Console -> Google Auth Platform -> Audience
estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: Both distributed builds have the same real Chrome extension ID and the owner has matched both Google client registrations to it."
    - "D-03: A real permitted managed-profile/personal-account pairing completes fallback and an owned-document operation."
    - "D-02 and D-04: A real revoked or expired grant produces Reconnect Google and an explicit tap restores access; Testing repeat consent remains documented."
    - "Automated regression success is recorded separately from live Console/account evidence; missing live evidence keeps phase acceptance pending."
  artifacts:
    - path: docs/OAUTH-RUNBOOK.md
      provides: Actual public ID/redirect/build hashes, owner completion record and privacy-safe live acceptance outcomes
    - path: manifest.json
      provides: Stable public key and owner-confirmed Chrome client identifier
      contains: '"key"'
    - path: lib/google.js
      provides: Owner-confirmed separate web client identifier
      contains: WEB_CLIENT_ID
  key_links:
    - from: docs/OAUTH-RUNBOOK.md
      to: manifest.json
      via: Observed Chrome ID, public key and Chrome OAuth client agree
      pattern: chromiumapp\.org
    - from: docs/OAUTH-RUNBOOK.md
      to: lib/google.js
      via: Owner confirms the exact redirect for WEB_CLIENT_ID
      pattern: WEB_CLIENT_ID
---
<objective>
Produce the reviewed build before requesting Ben's external action, then close AUTH-01/02/03 with actual Chrome and Google evidence per D-01 through D-04.

**As a** pilot friend on a school-managed Chrome account, **I want to** complete Google sign-in, **so that** my connection keeps working across reinstalls, reloads, and time.
</objective>
<execution_context>
@/Users/vanshkumar/.codex/gsd-core/workflows/execute-plan.md
@/Users/vanshkumar/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@LEARNINGS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-CONTEXT.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-02-SUMMARY.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-03-SUMMARY.md
@.planning/phases/01-stable-extension-id-oauth-hardening/COVERAGE.md
@docs/OAUTH-RUNBOOK.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Finish regression, both archives and concrete owner readiness</name>
  <files>docs/OAUTH-RUNBOOK.md</files>
  <read_first>Read both wave-2 summaries, package.json, packaging validator CLI, current runbook; inspect final diff for phase-owned auth/manifest changes and deferred scope boundaries.</read_first>
  <action>
After both wave-2 plans finish, run the complete required test chain, the focused real-panel OAuth smoke mode, and existing toolbar smoke. Run the unfiltered panel smoke diagnostically and compare its known baseline failures described in COVERAGE.md; record exact outcomes, investigate new regressions attributable to this phase, and do not rewrite unrelated clock/developer/chat behavior to green old assertions. Complete internal review and all accepted auth/security fixes before producing final archive hashes. Unresolved high/critical phase findings block the owner handoff.

Build both standard and friends ZIPs using the current package-store.sh. Derive paths from manifest.version, extract each into a separate new temporary directory, run the staged-package validator against each actual extracted archive, compare public keys/IDs with source, and verify the friends flag and existing viewer/offscreen files. Remove only task-owned temporary directories. Record exact file paths/hashes/public derived ID and automated case counts in the runbook.

Where available, use a separate test Chrome profile to load the keyed friends build and observe chrome.runtime.id and chrome.identity.getRedirectURL; compare to derivation. Do not alter or remove the existing student installation/data. If actual Chrome inspection is unavailable, leave those observations explicitly pending for Task 2, with exact instructions and the expected values ready. Keep owner/account checks pending until observed. Present the completed runbook and package paths before asking for the external action.
  </action>
  <acceptance_criteria>
    - Required regression chain, focused OAuth panel and toolbar checks pass with positive case counts; unfiltered panel baseline is documented distinctly.
    - Both final ZIPs pass validation after extraction and share source public identity; private material is absent.
    - Concrete public values, artifacts and hashes are ready; live observations are either recorded or clearly pending.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH /Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/pnpm --dir . run test</automated>
    <fails_when>Non-zero exit, any failed security/background/boundary/docops/auth/package assertion, or zero-case new suites.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/smoke-panel.js --google-auth</automated>
    <fails_when>Non-zero exit, missing executed OAuth-case count, any failed OAuth assertion, or absence of GOOGLE AUTH PASSED.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/smoke-toolbar.js</automated>
    <fails_when>Non-zero exit or absence of ALL PASSED.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH bash scripts/package-store.sh</automated>
    <fails_when>Non-zero exit, failed stable-key/credential check, or missing packaged (store) path and SHA-256 output.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH bash scripts/package-store.sh --friends</automated>
    <fails_when>Non-zero exit, failed stable-key/credential check, or missing packaged (friends) path and SHA-256 output.</fails_when>
  </verify>
  <done>Reviewed and tested standard/friends archives and the complete public-value runbook are ready before any user checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Ben matches both clients and verifies real account recovery</name>
  <reversibility rating="costly">D-01 registers the permanent extension identity; changing it later would require coordinated client and pilot-installation updates.</reversibility>
  <files>docs/OAUTH-RUNBOOK.md, manifest.json, lib/google.js</files>
  <read_first>Read prepared docs/OAUTH-RUNBOOK.md, final public identity/client values, archive hashes and recorded automated results.</read_first>
  <action>
This is the phase's final external gate because D-01 explicitly assigns Ben the Console changes. Ask for only the actions requiring his Google project/account access, after linking the completed runbook and friends archive. Ben observes the stable ID/redirect in Chrome, updates/verifies BOTH the Chrome Extension Item ID and separate Web application redirect, confirms pilot test-user setup and leaves the audience in Testing per D-04. Request sanitized outcomes/public replacement client IDs only, never passwords, tokens, secrets or student document contents.

If Console requires replacing a client, the executor applies only the returned public ID to manifest.oauth2.client_id or WEB_CLIENT_ID, preserves the permanent key/scopes, reruns all Task 1 gates, rebuilds both archives and updates hashes/runbook before continuing the same acceptance gate. Do not ask Ben to edit source files. A mismatched ID or redirect is a failure to correct, not evidence to waive.

Complete the runbook's real checks: stable ID across reload and second path/profile or machine; permitted school-profile blocked Chrome door → chosen personal account → owned test-document read or outline creation; revoke grant or use an expired Testing grant → silent failure gives Reconnect Google → explicit tap → restored document access; offline failure remains retryable and disconnect remains disconnected after reopen. Never remove the old install/data to achieve these checks. A denied school policy or unavailable account is recorded honestly and leaves the relevant requirement pending; mocks cannot substitute. No seven-day wait or production OAuth review is requested.

Record only date, profile category, build hash, public IDs and pass/fail observations in the runbook after the owner supplies evidence. Resume completion only once the required live outcomes are satisfied, otherwise return a concrete pending external action rather than marking Phase 1 verified.
  </action>
  <instructions>Use the prepared runbook's two-client configuration section and live acceptance table. Report which rows passed and any public replacement client IDs; the executor handles resulting source/configuration updates and rebuilds.</instructions>
  <verification>Compare owner-observed Chrome identity and exact redirect with both Console registrations and final package values. Record actual managed/personal connection and revoked/expired-grant recovery outcomes; any missing required observation keeps this checkpoint open.</verification>
  <resume-signal>Both clients match the observed ID/redirect and live acceptance rows have concrete outcomes; unresolved rows keep this checkpoint open.</resume-signal>
  <acceptance_criteria>
    - Both registrations are owner-confirmed against the observed permanent ID and exact redirect.
    - AUTH-01 has actual permitted managed/personal-account evidence; AUTH-02 has two-location identity evidence; AUTH-03 has revoked/expired-grant reconnect evidence and expected Testing behavior documentation.
    - Final archive hashes correspond to any owner-supplied client-ID updates; no secrets or old-install data enter evidence files.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-oauth-package.js</automated>
    <fails_when>Non-zero exit, zero executed cases or any invalid source-key/package-fixture assertion. Passing does not close the human gate.</fails_when>
    <human-check>Ben confirms both Console registrations and every required live row in the runbook; distinguish simulated/revoked grant from naturally elapsed seven days. Missing live evidence remains human_needed.</human-check>
  </verify>
  <done>Both real OAuth registrations and the actual permitted pilot connection/reconnect path are verified for the shipped permanent identity, with an accurate evidence record.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; high/critical findings block release. Boundaries: tested source → final package; owner Console → real OAuth service; student account → verification evidence.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14 | Tampering | Final release/configuration | high | mitigate | Task 1 validates actual extracted archives; every returned client-ID change reruns tests/build/hash checks. |
| T-01-15 | Spoofing | Wrong registered client/redirect | high | mitigate | Task 2 requires exact observed Chrome ID and getRedirectURL matching both owner-controlled registrations (V10). |
| T-01-16 | Information disclosure | Live acceptance record | high | mitigate | Store only public IDs, build hashes and sanitized outcomes; keep account/document/token/private-key data outside records (V14/V16). |
| T-01-17 | Elevation of privilege | Managed account policy | high | mitigate | Verify permitted use; preserve real denials as pending/failure, never instruct policy bypass (V8). |
</threat_model>
<verification>Required automated gates and owner-run real OAuth checks are separate. A healthy mock suite, recorded expected ID or pending checklist cannot satisfy the human gate. All source scope remains Phase 1.</verification>
<success_criteria>All AUTH requirements have actual evidence for the final package; Testing-mode repeat consent and old-install data retention are documented; outstanding live checks are never silently green.</success_criteria>
<output>Create .planning/phases/01-stable-extension-id-oauth-hardening/01-04-SUMMARY.md with final hashes, sanitized live outcomes and any remaining human_needed status. Do not mark this phase complete while the external gate is unresolved.</output>
