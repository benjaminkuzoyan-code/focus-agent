---
phase: 01-stable-extension-id-oauth-hardening
plan: "02"
type: execute
wave: 2
depends_on: [01-01]
files_modified: [lib/google.js, sidepanel/panel.js, scripts/test-google-auth.js, scripts/smoke-panel.js]
autonomous: true
requirements: [AUTH-01, AUTH-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: Confirmed authorization failure after eligible silent recovery changes the existing chip to Reconnect Google; network and resource errors do not claim grant expiry."
    - "D-03: Once the student selects the personal web door, invalidating its token does not silently switch to the school Chrome account."
    - "D-04: Testing-mode authorization lapse is expected pilot behavior and is recoverable through a deliberate chip click."
    - "Feature and background requests acquire and retry silently, with at most one unauthorized-response retry."
    - "Concurrent requests invalidate only the token and door they used; disconnect cannot be undone by a late connection response."
  artifacts:
    - path: lib/google.js
      provides: Silent request recovery, binary/JSON response support and shared terminal status
      exports: [FA.google.status, FA.google.disconnect, FA.google.getDoc, FA.google.fetchDriveFileBytes]
    - path: sidepanel/panel.js
      provides: Reconnect rendering, clear typed errors and guarded explicit reconnection
      contains: renderGoogleChip
    - path: scripts/test-google-auth.js
      provides: Retry, classification, provenance and race regression cases
    - path: scripts/smoke-panel.js
      provides: Feature-failure to reconnect-chip to restored access integration cases
  key_links:
    - from: lib/google.js
      to: scripts/test-google-auth.js
      via: Invalidate per-request token descriptor without clearing selectedDoor
      pattern: removeCachedAuthToken
    - from: lib/google.js
      to: sidepanel/panel.js
      via: googleAuthState onChanged event followed by storage-only render
      pattern: googleAuthState
    - from: lib/google.js
      to: scripts/test-google-auth.js
      via: Same silent one-retry policy with arrayBuffer response decoding
      pattern: arrayBuffer
  prohibitions:
    - statement: "MUST NOT present repeated pilot consent as student fault or promise permanent Google authorization while the app remains in Testing."
      status: unresolved
---

<objective>
Complete recovery without surprising sign-in windows or account changes, per D-02, D-03 and D-04.

**As a** pilot friend on a school-managed Chrome account, **I want to** complete Google sign-in, **so that** my connection keeps working across reinstalls, reloads, and time.
</objective>
<execution_context>
@/Users/vanshkumar/.codex/gsd-core/workflows/execute-plan.md
@/Users/vanshkumar/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@LEARNINGS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-CONTEXT.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-RESEARCH.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-01-SUMMARY.md
@.planning/phases/01-stable-extension-id-oauth-hardening/COVERAGE.md
</context>
<flagged_assumptions>
AUTH-03 is category:unclassified, status:unresolved in the deterministic edge report; mock expiry proves handling, not that seven actual days elapsed. The prohibition remains descriptor-less and flagged-unverified. A real revoked/expired grant check is mandatory in 01-04. Runtime error wording remains conservative until real sanitized observations support classification.
</flagged_assumptions>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Preserve account intent through bounded silent recovery</name>
  <files>lib/google.js, scripts/test-google-auth.js</files>
  <read_first>Read 01-01 auth contract/summary; lib/google.js api, token invalidation, isConnected, account, disconnect and fetchDriveFileBytes; background.js setInteractive(false) call; the new VM harness.</read_first>
  <behavior>
    - Expired access token with a live grant renews silently and stays connected; two eligible authorization failures yield reconnect only for a previously connected account.
    - First-run no-grant remains disconnected. Authorization plus network/unknown/configuration failure does not become an invented expired-grant verdict.
    - A first 401 evicts exactly its descriptor and retries once silently; a second 401 terminates, invalidates the rejected token and records reconnect.
    - 403 resource permission, 404 resource absence, 429, 5xx, timeout/offline, invalid client, unknown Chrome error and user cancellation retain their distinct categories without interactive escalation.
    - Selected web remains selected after 401/expiry; interleaved Chrome/web requests evict their own tokens; late failure cannot erase a newer successful token/status.
    - Drive bytes return an ArrayBuffer through the same policy; JSON and 204 retain their existing shapes.
  </behavior>
  <action>
Begin with failing cases against the real module. Refactor the existing api path into one authenticated request helper that accepts method/body and response mode. Preserve API payloads and all existing exported feature methods. Every acquisition and recovery from feature work must use interactive:false regardless of page context; only connect invoked by an explicit chip action may open UI. Preserve the worker setInteractive(false) contract.

Keep the complete acquired {token,door} descriptor attached to each request, eliminating mutable lastDoor as an invalidation authority. Web invalidation removes only the matching cached access token and keeps selectedDoor/previous connection metadata; compare token identity before deleting so a delayed response cannot evict a replacement. Chrome invalidation passes that request's exact token to removeCachedAuthToken. Tie recovery/status writes to the current connection generation and ignore superseded completions, including disconnect/account-change events observed through storage.

Per D-02, retain both causes where both doors are eligible. Only confirmed authorization-required outcomes across those eligible attempts, or terminal 401 after one silent retry, create reconnect for an existing connection. A deliberately selected web account has one eligible door; D-03 does not authorize switching it silently to Chrome. Configuration errors need owner action; policy denials, cancellation, resource failures and transient/unknown failures must not be relabeled expiry. Use bounded sanitized categories, and preserve prior status for retryable failures. Do not set a seven-day client countdown; respond to provider outcomes per D-04.

Route fetchDriveFileBytes through this helper with binary decoding, including bounded recovery. Preserve cookie-based mobilebasic/export readDoc paths and student/developer write boundaries. Never retry an ambiguous network failure for mutating Docs/Calendar operations; only the existing definite unauthorized-response retry is eligible. Disconnect revokes best effort over the existing HTTPS endpoint, clears cached credentials/selection/status even offline, and blocks late success from restoring them. Revocation failure does not keep the local user connected.
  </action>
  <acceptance_criteria>
    - All Google bearer API calls share one recovery policy; binary responses retain bytes and resource diagnostics.
    - Every feature/background identity invocation recorded by tests has interactive:false; request count never exceeds two for a 401 sequence.
    - A revoked saved-web grant never triggers silent Chrome-account adoption; per-request token eviction survives deliberate out-of-order callbacks.
    - Cancellation/transient/resource/setup failures have separate tested outcomes; status contains no tokens or raw errors.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-google-auth.js</automated>
    <fails_when>Non-zero exit, zero executed cases, missing recovery/provenance/race assertions, or any FAIL assertion.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-background.js</automated>
    <fails_when>Non-zero exit or any failing background assertion.</fails_when>
  </verify>
  <done>Every Google feature, including Drive downloads, can renew a valid grant silently or emit a typed terminal result without switching the student's selected door or opening a chooser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover a failed feature through the existing reconnect chip</name>
  <files>sidepanel/panel.js, scripts/smoke-panel.js, scripts/test-google-auth.js</files>
  <read_first>Read current Google chip renderer/click listener and chrome.storage.onChanged branch; existing sourceNotice handling; completed Task 1 error/status contract; real panel smoke boot and Google fixtures.</read_first>
  <behavior>
    - A real-module feature failure persists reconnect and updates the visible chip without closing the panel; reload keeps that state.
    - Reconnect Google has an accessible label/title, is distinct by words, and opens one chooser only after a click; success clears its marker and restores getDoc access.
    - A cancelled reconnect retains its prior reconnect state; a network/resource failure does not change the chip to a false expiry notice.
    - Explicit disconnect yields disconnected after reload, including failed network revocation; rendering status changes neither probes identity nor loops storage writes.
  </behavior>
  <action>
Write the real panel smoke assertions first. Expand the renderer to connected, disconnected and Reconnect Google using the existing button and note; no new surface or layout system. Set title/aria-label consistently, use textContent, preserve the pending guard through finally, and handle status storage deletion as well as updates. Connected clicks retain the existing disconnect confirmation; reconnect/disconnected clicks call connect directly rather than making a fresh isConnected probe that might obscure the needed action.

Use the shared typed error category for short student-facing copy: authorization asks for reconnect, offline/server failure asks to retry, configuration asks the owner to check setup, and resource denial explains access belongs to the chosen account. Cancellation is not a scary error. Keep provider payloads, token fragments and raw diagnostic strings out of UI/console/coach data. Per D-02 and D-04, explain in the existing note/tooltip that the pilot may ask for Google consent again; avoid blame or claiming an exact elapsed duration. Existing amber styling may be reused if available; words must carry the distinction without a CSS dependency.

Extend smoke-panel's synthetic identity/fetch sequences to drive getDoc failure through storage notification into the actual chip, then click reconnect and prove feature access returns. Cover repeated click, reload/persisted state, disconnect, cancellation and offline cases, while preserving all existing panel regressions. Account display may show a verified optional email or a generic personal/Chrome label; an empty email is valid.
  </action>
  <acceptance_criteria>
    - Reconnect is visible immediately after a terminal feature auth failure and after boot with persisted reconnect state.
    - A single explicit click restores the connection and clears the reconnect marker; no callback triggers UI on its own.
    - Real production renderer, listeners and Google module participate in smoke cases; status rendering has zero identity calls.
    - Existing student/developer write boundaries and unrelated storage listeners remain intact.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/smoke-panel.js --google-auth</automated>
    <fails_when>Non-zero exit, any failed OAuth assertion, zero cases, missing reconnect/reload/recovery assertions, or absence of GOOGLE AUTH PASSED.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-panel-boundary.js</automated>
    <fails_when>Non-zero exit, zero passed cases, or any FAIL assertion.</fails_when>
  </verify>
  <done>A student can recover Google access through a clear deliberate action on the existing chip, and non-auth failures retain honest diagnostics.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; high/critical findings block acceptance. Boundaries: provider response → account/token state; worker → identity UI; cross-context storage → visible status.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Spoofing | api/forgetToken | high | mitigate | Task 1 binds invalidation to request descriptor and preserves selected web account intent (V6/V7/V8). |
| T-01-06 | Denial of service | Silent recovery | medium | mitigate | One 401 retry; every feature recovery silent; no ambiguous network replay of writes (V2/V10). |
| T-01-07 | Information disclosure | Status and chip errors | high | mitigate | Finite sanitized error categories, textContent, no callback/token/provider text in status, UI or console (V14/V16). |
| T-01-08 | Tampering | Late connect/disconnect response | medium | mitigate | Connection generation plus current-token checks and storage invalidation; race fixtures cover stale completions (V7). |
| T-01-09 | Elevation of privilege | Existing Docs operations | high | mitigate | Preserve existing feature permission/student gates and run test-panel-boundary.js; no expanded scopes or writing methods. |
</threat_model>
<verification>Task gates plus final full regression in 01-04. Real revoked/expired grant and managed-account tests remain external acceptance. No dependency installation is introduced by this plan.</verification>
<success_criteria>Silent renewal, account continuity, per-request invalidation, accurate reconnect UI and typed non-auth failures pass real-source automated cases.</success_criteria>
<output>Create .planning/phases/01-stable-extension-id-oauth-hardening/01-02-SUMMARY.md with test counts, status/error contract and unresolved live verification.</output>
