---
phase: 01-stable-extension-id-oauth-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - lib/google.js
  - sidepanel/panel.js
  - scripts/test-google-auth.js
  - scripts/smoke-panel.js
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: A single RSA public key fixes the extension identity; the private key remains outside version control and distribution."
    - "D-03: Explicit connection tries Chrome first and proceeds directly to the personal-account chooser for the known disabled-account response."
    - "D-02: Successful explicit connection persists a shared status that the existing Google chip can render after reload."
    - "A web authorization response is accepted only for its own unpredictable state and exact expected redirect."
  artifacts:
    - path: manifest.json
      provides: Permanent public SubjectPublicKeyInfo key with unchanged OAuth scopes
      contains: '"key"'
    - path: lib/google.js
      provides: Typed OAuth boundary, token provenance and durable status reader
      exports: [FA.google.connect, FA.google.status, FA.google.account, FA.google.isConnected, FA.google.disconnect, FA.google.setInteractive]
    - path: sidepanel/panel.js
      provides: Existing Google chip driven by shared status and explicit click intent
      contains: renderGoogleChip
    - path: scripts/test-google-auth.js
      provides: Real-module VM regression harness with synthetic identity and network effects
    - path: scripts/smoke-panel.js
      provides: Real panel boot, Google-chip click and authenticated Docs read tracer
  key_links:
    - from: manifest.json
      to: lib/google.js
      via: Permanent public key plus existing oauth2 configuration consumed through runtime.getManifest and identity.getRedirectURL
      pattern: key
    - from: sidepanel/panel.js
      to: lib/google.js
      via: Google chip click calls FA.google.connect and renders FA.google.status
      pattern: FA\.google\.(connect|status)
    - from: lib/google.js
      to: sidepanel/panel.js
      via: googleAuthState status record; separate existing googleWebToken credential record
      pattern: googleAuthState
    - from: scripts/test-google-auth.js
      to: lib/google.js
      via: Real launchWebAuthFlow boundary exercised with per-attempt state and redirect verification before any credential write
      pattern: launchWebAuthFlow
  prohibitions:
    - statement: "MUST NOT present personal Gmail as permission to bypass school policy or access documents that account cannot view."
      status: unresolved
---

<objective>
Deliver the keyed extension's explicit Google connection path through existing UI, identity APIs and local storage, then secure its personal-account fallback per D-01, D-02 and D-03.

**As a** pilot friend on a school-managed Chrome account, **I want to** complete Google sign-in, **so that** my connection keeps working across reinstalls, reloads, and time.

Purpose: prove the existing extension architecture end to end before expanding recovery behavior. Output: permanent public key, shared auth contract, connected chip and executable regressions. Live Console configuration remains 01-04 acceptance, not a claimed result of these tests.
</objective>

<execution_context>
@/Users/vanshkumar/.codex/gsd-core/workflows/execute-plan.md
@/Users/vanshkumar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@LEARNINGS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-CONTEXT.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-RESEARCH.md
@.planning/phases/01-stable-extension-id-oauth-hardening/01-PATTERNS.md
@.planning/phases/01-stable-extension-id-oauth-hardening/COVERAGE.md
@docs/SPEC.md
</context>

<interfaces>
Existing code is a plain browser IIFE exporting FA.google, loaded by panel.html and background.js. Preserve public method return shapes for connect (true), isConnected (boolean), account ({door,email} or null), disconnect, setInteractive and all Docs/Drive/Calendar methods. Internal getToken returns a per-acquisition descriptor {token,door}; callers must keep that descriptor through invalidation. Add FA.google.status() as a storage-only, side-effect-free reader. Use googleAuthState with version:1, status:disconnected|connected|reconnect, selectedDoor:chrome|web|null, everConnected:boolean and a bounded reason or null. Keep existing googleWebToken compatibility; never put bearer tokens or raw callback/provider strings in the status record. Email remains optional display metadata, never evidence inferred from a prior different token. Invalidate tokens separately from selectedDoor.
</interfaces>

<assumption_delta_decision>
Primary noun: shared Google auth coordinator. Decision: no-change. The scan detected fallback, but both doors already exist in lib/google.js; this phase preserves their shared coordinator and makes token provenance explicit instead of introducing another primary account system.
</assumption_delta_decision>

<flagged_assumptions>
AUTH-01 remains category:unclassified, status:unresolved from /tmp/focus-auth-edges.json; its managed-profile feasibility is not resolved by this plan. AUTH-02 and AUTH-03 are retained in 01-03 and 01-02 respectively. The descriptor-less prohibition above is flagged-unverified, not a wired check or a passed finding. See COVERAGE.md for the complete recall and source audit.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keyed install → explicit Chrome connection → persisted chip → Docs read</name>
  <reversibility rating="costly">D-01 fixes a public identity; reversing it requires coordinating both OAuth registrations and every pilot installation.</reversibility>
  <files>manifest.json, lib/google.js, sidepanel/panel.js, scripts/test-google-auth.js, scripts/smoke-panel.js</files>
  <read_first>Read manifest.json and .gitignore; lib/google.js auth and exported account functions; sidepanel/panel.js Google chip and local storage listener; scripts/test-panel-boundary.js VM pattern; scripts/smoke-panel.js boot and effect mocks; package.json and package-lock.json; research Package Legitimacy Audit.</read_first>
  <behavior>
    - Actual panel click connects once through a fake successful Chrome identity callback; shared status persists; chip shows connected; FA.google.getDoc completes through the real module with that token.
    - Reopening a panel against the persisted status shows connected without initiating an interactive flow.
    - Clean first run shows disconnected, with no chooser until a click; simultaneous clicks share one pending connection.
    - Manifest key is valid public DER and derives a deterministic ID; all existing OAuth scopes stay identical.
  </behavior>
  <action>
First establish failing VM and actual-panel smoke assertions for this one path. Use the bundled Node bin directory on PATH for this plan. The orchestrator has restored audited jsdom 25.0.1 from the existing lockfile with scripts disabled, confirmed resolution, and preserved package.json/package-lock.json. No installation task remains. Report a genuine changed-environment failure rather than making the focused smoke optional.

Per D-01, inspect whether a valid manifest key or an owner-supplied private key already exists before creating anything. Reuse an established pair; never overwrite or rotate it automatically. For an unkeyed checkout without a reusable pair, use OpenSSL to generate one RSA-2048 pair in a new ignored .pem file at repo root with exclusive creation and owner-only permissions; export its SubjectPublicKeyInfo public DER as one-line base64 into top-level manifest.key. Verify git check-ignore covers the private path and git ls-files does not track it; keep the private bytes out of commands' output and test fixtures. Existing *.pem exclusion already satisfies D-01. Hash public DER with SHA-256, take its first 16 bytes and map hexadecimal nibbles 0-f to a-p for the expected extension ID. Real Chrome corroboration follows in 01-04. Keep current client IDs/scopes intact until the owner supplies verified replacements, if any.

Introduce the interfaces above inside the existing lib/google.js IIFE. Classify identity boundary failures while callback-scoped runtime.lastError is available. Define finite error categories authorization, policy_disabled, cancelled, network, configuration, resource, unknown; carry sanitized category/door details, not provider strings into UI or storage. Migrate existing saved-web metadata lazily to selectedDoor:web without deleting credentials; treat expired access tokens as unverified until a silent probe resolves them. connect is the only normal interactive entry; retain setInteractive(false) as a worker guard and preserve existing callers. Implement the successful Chrome path and compatible status/account reads; token provenance must already be attached to every acquired token, including the existing web path.

Extend the existing chip renderer to read status, using textContent and current markup. Use a pending guard and a narrowly filtered googleAuthState storage event branch. Update misleading comments about token storage. Extend existing smoke mocks with realistic manifest data, identity methods, removal events, controlled fetch and Web Crypto; boot real panel.html scripts, click the actual button, perform a real-module getDoc using synthetic provider responses and check persisted/rendered state. Add a --google-auth mode that runs these actual-panel OAuth cases, prints a positive executed-case count and GOOGLE AUTH PASSED, and exits nonzero on any failure or zero cases. The unfiltered smoke retains its existing assertions and reports the seven pre-existing failures listed in COVERAGE.md separately; do not fix unrelated clock/developer/chat behavior. This is a runnable extension slice, not a new app, service, database or deployment.
  </action>
  <acceptance_criteria>
    - scripts/test-google-auth.js exists, executes the real lib/google.js in isolated VM contexts, prints a positive executed-case count and exits nonzero on failed or zero assertions.
    - The smoke gate exercises the actual Google button handler, storage notification and renderer, then getDoc; it does not replace FA.google with a fake.
    - One permanent public key is committed; the private key is ignored, untracked and never printed.
    - Existing feature exports and three Google scopes remain compatible; boot and rendering have no interactive effect.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-google-auth.js</automated>
    <fails_when>Non-zero exit, missing executed-case summary, zero executed cases, or any FAIL assertion.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/smoke-panel.js --google-auth</automated>
    <fails_when>Non-zero exit, any failed OAuth assertion, zero executed cases, or absence of both the tracer assertion and GOOGLE AUTH PASSED.</fails_when>
  </verify>
  <done>The keyed extension's explicit Chrome connection persists and renders through the real panel, and an authenticated Docs read succeeds in the deterministic integration harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Secure and exercise the personal-account fallback path</name>
  <files>lib/google.js, scripts/test-google-auth.js, scripts/smoke-panel.js</files>
  <read_first>Read Task 1's exported auth contract and tests; lib/google.js getWebToken, getToken and WEB_CLIENT_ID; research callback-validation and error-classification sections; current Google chip integration smoke.</read_first>
  <behavior>
    - Explicit Chrome policy-disabled failure leads to exactly one interactive web flow; a silent caller never escalates its interaction flag.
    - Every new web attempt has distinct cryptographic state; only exact origin/path/state and a well-formed bearer response with finite positive lifetime can be accepted.
    - Missing, mismatched, duplicated state, wrong origin/path, token-plus-error, missing token, invalid token type, or malformed lifetime cause zero credential/status success writes and zero authenticated feature requests.
    - OAuth error responses also require valid redirect/state before classification; cancellation preserves any prior working connection.
    - A prior selected web door remains selected after access-token expiry; optional email lookup cannot reuse stale display identity after explicit account selection.
  </behavior>
  <action>
Start with failing tests. Per D-03 keep Chrome-first for an unselected account and selected-web preference for a prior personal account. The exact known disabled-account response bypasses additional Chrome retries and goes straight to web in the same explicit connect operation; retain conservative handling for unfamiliar Chrome messages. Apply the caller's interaction policy unchanged to both doors. Coalesce concurrent explicit connects and use a connection generation guard so a cancelled/disconnected superseded attempt cannot later write success.

Harden getWebToken with a fresh crypto.getRandomValues nonce per authorization attempt, held only in that attempt. Add state to the Google authorization URL. Parse the returned URL with native URL/URLSearchParams; require the exact getRedirectURL origin and pathname, reject unexpected query parameters/credentials, and require one exact matching fragment state before interpreting either tokens or errors. Reject ambiguous response fields and invalid or missing expires_in/token_type rather than supplying a fabricated lifetime. Read scopes from the existing manifest; retain the locked implicit web flow and best-effort userinfo request without adding scopes or a client secret.

Persist successful selectedDoor and credential separately. Preserve any independently confirmed account hint during silent renewal; never carry an old email forward as proof of a newly selected account. If the provider reports an identity conflicting with a known selected identity, discard that token and require explicit selection instead of silently adopting it. Optional email absence must not turn a valid grant into a display failure. Test both success and rejection side effects. Add the real-panel disabled-Chrome → personal-web → connected → getDoc smoke path, using the outgoing state's value in the synthetic valid callback. Policy denial copy must respect permitted account use; personal Gmail does not confer school-document permissions.
  </action>
  <acceptance_criteria>
    - Disabled Chrome account causes one web chooser on an explicit click and none during a silent request.
    - Invalid callback fixtures never reach credential storage or feature fetch; accepted callbacks bind to their initiating attempt.
    - Web-account preference survives access-token expiry; errors preserve both attempted-door classifications where both are eligible.
    - Both Chrome and web chip paths are exercised against real production source in the smoke suite.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/test-google-auth.js</automated>
    <fails_when>Non-zero exit, zero executed cases, missing callback/fallback cases, or any FAIL assertion.</fails_when>
    <automated>PATH=/Users/vanshkumar/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH node scripts/smoke-panel.js --google-auth</automated>
    <fails_when>Non-zero exit, zero executed OAuth cases, missing personal-account tracer assertion, or absence of GOOGLE AUTH PASSED.</fails_when>
  </verify>
  <done>Permitted personal-account fallback works through the existing UI and validated OAuth boundary, with test-proven caller intent and account-door continuity.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; unresolved high/critical findings block phase acceptance. Boundaries: user click → extension auth; Google/Chrome callback → trusted credential store; source manifest → installed identity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | getWebToken callback | high | mitigate | Task 2 checks unpredictable per-attempt state and exact redirect before success/error processing; rejects ambiguous response shape (V2/V10). |
| T-01-02 | Information disclosure | Private key/status/UI | high | mitigate | Task 1 uses exclusive private-key creation, verifies ignore/untracked status, ships only public DER, and persists bounded status without bearer/provider strings (V11/V14/V16). |
| T-01-03 | Denial of service | Connect actions | medium | mitigate | Pending guard, coalesced connection and explicit-only interactive calls; regression assertions inspect call options (V6/V7). |
| T-01-SC | Tampering | Existing restored test dependency | high | mitigate | Use the already restored/audited jsdom 25.0.1 from the existing lockfile; no new installs, package additions or lockfile churn. |
| T-01-04 | Information disclosure | Inherited googleWebToken local store / implicit flow | medium | accept | Preserve existing architecture under D-03; avoid new token copies; document legacy implicit flow and content-script storage boundary in 01-03. |
</threat_model>

<verification>
Run both task gates after each changed slice. The VM and jsdom suites execute real source against synthetic services; they cannot prove Google Console configuration or an actual managed account. New install identity and external grant behavior are required by 01-04. No Nyquist scaffold, UI-SPEC, new framework or database is required for this bounded brownfield slice.
</verification>

<success_criteria>
Permanent public identity, explicit Chrome/personal connection, validated web callbacks and shared connected status are implemented and exercised end to end; no sensitive status values or automatic chooser path is introduced.
</success_criteria>

<output>
Create .planning/phases/01-stable-extension-id-oauth-hardening/01-01-SUMMARY.md with symbols, test counts, public derived ID and private-key custody location only (never private bytes). Record real Google verification as pending 01-04.
</output>
